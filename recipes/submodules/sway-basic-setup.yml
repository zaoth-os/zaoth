# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    install:
      packages:
        # Majors
        - swayfx
        - pamixer
        - noctalia
        - brightnessctl

      #install-weak-deps: false
  - type: dnf
    install:
      packages:
        # Minors
        - ranger
        - dunst
        - i3blocks
        - gtklock
        - nwg-look

      install-weak-deps: false
