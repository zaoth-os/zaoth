# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:

  - type: dnf
    install:
      packages:
        - greetd
        - tuigreet
      install-weak-deps: false

  - type: systemd
    user:
      enabled:
        - display-manager
