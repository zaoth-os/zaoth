# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  # Last Custom Scripts
  - type: script
    scripts:
      - greeter_dm.sh

  # Initramfs rebuild
  - type: initramfs
